name: Diagnose Bot

on:
  workflow_dispatch:

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: PM2 status + recent logs
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          command_timeout: 2m
          script: |
            set +e
            cd ~/whatsapp-bot
            echo "=== PM2 list ==="
            pm2 list
            echo
            echo "=== PM2 info ==="
            pm2 info whatsapp-bot
            echo
            echo "=== Session DB present? ==="
            ls -la neonize.db 2>&1
            echo
            echo "=== Python version ==="
            python3 --version
            echo
            echo "=== Last 200 OUT lines ==="
            pm2 logs whatsapp-bot --lines 200 --nostream --out
            echo
            echo "=== Last 100 ERR lines ==="
            pm2 logs whatsapp-bot --lines 100 --nostream --err
