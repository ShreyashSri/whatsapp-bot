name: Deploy WhatsApp Bot

on:
  push:
    branches: [main]

jobs:
  docker:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            shreyashsri/whatsapp-bot:latest
            shreyashsri/whatsapp-bot:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest
    needs: docker
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Copy files to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          timeout: 30s
          source: "bot.py,requirements.txt,features,cards"
          target: ~/whatsapp-bot

      - name: Install deps and restart bot
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          command_timeout: 10m
          script: |
            # Load user profile
            source ~/.profile || source ~/.bashrc || source ~/.zshrc || true
            
            cd ~/whatsapp-bot
            
            # Create a virtual environment if it doesn't exist
            python3 -m venv venv
            
            # Use the virtual environment's pip to install dependencies safely
            venv/bin/python -m pip install --upgrade pip
            venv/bin/pip install -r requirements.txt
            
            # Install Playwright browsers
            venv/bin/playwright install chromium
            
            # Start/Restart the bot in the background using PM2
            npx pm2 restart whatsapp-bot 2>/dev/null || npx pm2 start "venv/bin/python bot.py" --name whatsapp-bot
            npx pm2 save
