{
  "name": "ctf-whatsapp-stats-bot",
  "version": "1.0.0",
  "description": "Persistent WhatsApp bot — nightly CTF stats + on-demand via 'Statistics' keyword",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "send-once": "node send-stats.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "mongodb": "^6.0.0",
    "node-cron": "^3.0.0",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.23.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
